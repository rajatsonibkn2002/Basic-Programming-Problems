#include <stdio.h>
void main()
{
    int length, pages;
    scanf("%d %d", &length, &pages);
    if(length<=23 && 500<pages<1000)
    {
        printf("Take Medicine");
    }
    else
    {
        printf("Don't take Medicine");
    }
}
