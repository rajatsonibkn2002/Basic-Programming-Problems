#include <stdio.h>
void main()
{
    int N, X, Y, i;
    scanf("%d", &N);
    scanf("%d", &X);
    for(i=1; i<=N; i++)
    {
        scanf("%d", &Y);
        if(Y>=X)
        {
            printf("YES\n");
        }
        else
        {
            printf("NO\n");
        }
    }
    
}
