#include <stdio.h>
#include <string.h>
int main()
{
    char s[100];
    int a, sum=0, i, j, b, l;
    gets(s);
    l = strlen(s);
    for(i=0; i<l; i++)
    {
        a = s[i];
        for(j=97; j<=122; j++)
        {
            if(a==j)
            {
                a = a-96;
            }
        }
        sum = sum + a;
    }
    printf("%d", sum);
 
}
