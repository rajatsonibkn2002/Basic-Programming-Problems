#include <stdio.h>
void main()
{
    int N;
    scanf("%d", &N);
    int i, A[N];
    for(i=0; i<N; i++)
    {
        scanf("%d", &A[i]);
    }
    for(i=0; i<N; i++)
    {
        printf("%d ", A[i]);
    }
    
}
