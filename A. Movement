#include <stdio.h>
void main()
{
    int n, i, stp =0;
    scanf("%d", &n);
    for(i=5; i>=1; i--)
    {
        while(n>=i)
        {
            stp = stp + (n/i);
            n = n%i;
        }
        
    }
    printf("%d", stp);
    
}
