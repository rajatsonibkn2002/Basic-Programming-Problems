#include <stdio.h>
void main()
{
    int large, i, A[3];
    for(i=0; i<3; i++)
    {
        scanf("%d", &A[i]);
    }
    large = A[0];
    for(i=0;i<3;i++)
    {
        if(A[i]>large)
        {
            large = A[i];
        }
    }
    printf("%d", large);

}
